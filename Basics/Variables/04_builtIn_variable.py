# This is the broadest and outermost scope.
# It contains all of Python's pre-defined functions, exceptions, and keywords 
# (e.g., print(), len(), int(), NameError).
# These built-in names are always available and can be accessed 
# from any part of the program without explicit import.

print("Hello") # print is a built-in function